{
  "rules": {
    "alpha": {
      ".read": true,
      ".write": "auth != null",
      "birdEventsMap": {
        ".indexOn": ["syncedAt", "date", "species"]
      }
    },
    "prod": {
      ".read": true,
      ".write": "auth != null",
      "birdEventsMap": {
        ".indexOn": ["syncedAt", "date", "species"]
      }
    },
    "constants": {
      ".read": true,
      ".write": "auth != null"
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
